[package]
name = "alligregator"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "3.0.14", features = ["derive"] }

[profile.release]
lto = true
panic = "abort"
codegen-units = 1
